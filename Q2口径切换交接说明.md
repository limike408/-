# Q2 口径切换交接说明（给下一位队友）

> 本分支：`Q2 口径切换全部完成并通过验收`（基于 `limike408/code`，已推送 origin）
> 一句话：**Q2 官方结果从"完美信息（全年紧急购电=0、表3 全空）"切换为"预报式两阶段随机规划"，表3 填真数；代码、结果、验收全部完成。**

---

## 一、改了什么、为什么

- **旧口径的问题**：把附件2 实际负荷/光伏当作 0:00 计划依据 → 全年 Q≡0 → `result2.xlsx` 紧急购电量表全空、论文表3 全零（评审风险）。
- **新口径**：0:00 用"截至前一日的历史数据"造预报（信息因果，不含未来）→ 两阶段随机规划定计划 P → 固定 P 拿当日实际结算 → 得到真实 Q。**Q2 与 Q3 从此口径可比**（Q2=只有 0:00 一次预报、不可调整；Q3=0/6/12/18 四次预报、可调整）。
- **已拍板的参数**（勿擅自改，改动需全队确认并重跑）：
  | 项 | 值 |
  |---|---|
  | 点预报 | 负荷/光伏各自取前 K 天逐槽（144 时段）均值 |
  | 情景生成 | 整日成对残差 bootstrap：残差 = 历史每日(实际 − 点预报)，按整天有放回抽 S=30（负荷+光伏同一天成对） |
  | 抽样种子 | 2026 + 日序（确定性、重跑逐位一致） |
  | K 选择 | 候选 {7,15,30}，全年交叉验证（归一化 RMSE）→ **K_L=15，K_P=7** |
  | 计划 P | `lp.solve_two_stage(lam=0, beta=0.90)`，纯期望最小化 |
  | 结算 | `lp.solve_day_fixedP`（紧急价 5p），E_end 结转次日 |
  | 储能口径 | E∈[1200,10800]、充放 ≤833.33 kWh/时段、η 充放各 90%（往返 81%）、2025-01-01 E=6000、跨日连续 |
  | 输出区间 | 2025-02-01~12-31（1 月仅作储能预热） |
  | 冷启动 | 2025-01-01 无历史 → 用附件1 典型日负荷/光伏剖面作情景基准 |

## 二、核心结果（教练已逐项验收）

| 指标 | 数值 |
|---|---|
| 全年计划购电费 | **15,133,792.26 元** |
| 全年紧急购电费 | **1,074,587.33 元**（346,164 kWh） |
| 全年合计 | **16,208,379.58 元** |
| 旧完美信息口径（备份） | 12,259,844.62 元 |
| 新旧差 | +2,873,947.63 元（保险性多购：把缺电风险从 5 倍紧急价转移到 1 倍计划价） |

**K 交叉验证（全年平均归一化 RMSE）**：

| K | 负荷 | 光伏 |
|---|---|---|
| 7 | 0.217367 | **0.117933** ← K_P |
| 15 | **0.213626** ← K_L | 0.130183 |
| 30 | 0.219628 | 0.163186 |

**表3 四指定日期**（明细 144 时段见 `report/q2_specdays_Q.csv`）：

| 日期 | 星期 | 紧急购电量 | 紧急费 |
|---|---|---|---|
| 2025-03-20 | 四 | 58.7 kWh | 320.4 元 |
| 2025-06-21 | **六** | **0 kWh（合法，见待办#2）** | 0 元 |
| 2025-09-23 | 二 | 2,562.6 kWh | 7,895.3 元 |
| 2025-12-21 | 日 | 1,102.3 kWh | 2,862.7 元 |

**λ 敏感性（新情景生成器，`report/q2_two_stage_comparison.csv`）**——方向是教科书式"保险"：λ↑ → 计划费↑、紧急费↓。高风险日总费用下降（9-23：47,976→46,261；12-21：62,424→60,795），低风险日总费用上升（3-20：42,582→44,125；6-21 无风险可保）。**注意：论文旧 λ 表方向相反（那是旧逐槽独立抽样的假象），CVaR 小节须按新表重写。**

## 三、交付物清单

- `report/result2.xlsx`：新官方结果（计划购电量 334 天、紧急购电量 3,906 条非零记录、充放电量 2,672 行；已清除模板个人元数据）
- `report/result2_完美信息备份.xlsx`：旧口径备份（保留，勿删）
- `report/q2_new_summary.csv`、`report/q2_specdays_Q.csv`、`report/q2_K_cv.csv`、`report/q2_two_stage_comparison.csv`
- `report/figures/q2_cvar_tradeoff.png`
- `report/q2_digest.txt`：跨次运行逐位一致性锚点（`fc641e4e…`）
- 代码改动：`code/scenarios.py`（新增 `residual_scenarios`）、`code/main_q2.py`（重写官方循环 + CV + digest + 冷启动）、`code/results.py`（新增 `strip_personal_meta`）、`code/config.py`（**路径改为相对仓库根自动定位，任何机器 clone 后直接可跑**）
- `handoff.md`：执行 Agent 收到的原始任务书

## 四、复现命令

```powershell
cd code
python -X utf8 main_q2.py    # ≈5.5 分钟，每 30 天打印一行进度；重跑触发 digest 校验
```

## 五、已知特性与待办（请下一位队友接力）

1. **【已知特性】全年日末储电量恒为 1200 kWh**：Q2 无日末终端价值 + 固定 P 结算下，电池成为"免费日内风险缓冲"，每天被榨干到运行下限。论文需一句话解释（或下一版给日末 SOC 加"明日价值"项，属模型升级）。
2. **【待办·周末效应】6-21（周六）Q=0**：负荷存在工作日/周末二值性，15 天窗口被工作日主导 → 周末日计划多购、靠弃光吸收。建议：点预报加**星期哑变量**（或工作日/周末分均值），重跑对比全年 NRMSE 与表3。
3. **【待办·季节】季节分层选 K 未实现**（handoff 原要求，本版先跑通全局 K）：做"全局 K vs 分季 K"对比——季节按月份（冬 12/1/2、春 3/4/5、夏 6/7/8、秋 9/10/11），每季各自交叉验证选 K；若分季全年 NRMSE 更低则采用并重跑。可与 `season_split.py` 衔接，与 #2 可合并为"特征增强点预报"。
4. **【待办·论文更新】main.tex 与 paper/结果说明.md、README/项目说明 里的旧 Q2 数字**（12,259,844.6、紧急 0、旧 λ 表）全部换新；摘要 Q2 段、表1/表2/表3、CVaR 叙事重写；论文须写明：整点预报插值口径、η 双向 90% 假设、冷启动、K 交叉验证。
5. **【待办·元数据】result1/3/4-2/4-3.xlsx 仍带模板作者字段**（creator="xueyi"），提交前用 `R.strip_personal_meta()` 清一遍。
6. **【下一阶段·Q3】**（与 Q2 口径统一相关，教练已预研）：
   - Q3 目前**负荷按当日已知**处理，与 Q2 的预报式负荷口径不一致 → 需统一（负荷也用历史预报）；
   - 滚动调整时点的 SOC 初值用的是"计划轨迹"而非"按已实现光伏重放的实际轨迹"；
   - 结算用全天实际光伏重优化电池 = 后见之明（非因果），实际调度只能用到当时信息；
   - 论文标题自称"随机 MPC"，但 Q3 实际是确定性滚动：`scenarios.py` 里的 `pv_scenarios()` 是死代码——可启用后把调整决策做成情景鲁棒，让"随机 MPC"名副其实。
7. **【git】本分支已推送 origin；`main` 未动**（定稿后再讨论合并）；协作规范见 `README.md`。

## 六、验收记录（教练核验）

- [x] 全年紧急购电费 1,074,587.33 > 0，量级合理（346,164 kWh，平均约 3.1 元/kWh，与附件1 峰谷价一致）
- [x] 表3 四日 3/4 非零；6-21=0 已诊断（周末效应），转待办 #2
- [x] 每日储能 E ∈ [1200,10800]（程序内断言，365 天全过）
- [x] 可复现：4 次运行 digest 逐位一致（执行 Agent 3 次 + 教练 1 次）
- [x] K_L=15、K_P=7 明确打印，CV 口径因果（只用当日之前数据）
- [x] 新旧差异 +287.4 万有合理解释
- [x] 合规：lp.py/data.py 零改动；paper/ 未动；无擅自 git 操作
- [x] 修复记录：冷启动规格偏差（原实现首日全零预报→电池躺平）已按拍板规格修复；修复后官方数字不变（1 月预热吸引子一致），digest 与修复前相同

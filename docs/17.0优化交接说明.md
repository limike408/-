# 17.0 优化交接说明（给下一位队友）

> 分支：`17.0优化`（基于 `16.0优化`）
> 一句话：**修复第三方审查发现的附件时序环移 10 分钟根因缺陷（B1），全链重跑刷新全部数字，
> 并完成附带修复；剩余论文文字/内容类缺陷（第二批）列于 §二，请下一位队友按清单接力。**
> 审查缺陷总账见会话核查结论（B1–B9 + 新增 7 条 + 表9/A12 漏 Q4-2，共 17 条），
> 本批完成其中"数字/根因"类，其余为文字/内容类。

---

## 一、本轮已完成（17.0：列序修复批次）

### 1. 根因修复（B1，最致命）
- **缺陷**：`code/data.py` 的排序函数把 `'0:00+1'`（24:00）标签剥掉 `+1` 后当 0 点排序 →
  附件1（行）与附件2/4（列）全部**环移 10 分钟**（三份附件都错位，不止附件1）。
  表现为：Q1 的 P 是"环移世界"的解；Q2–Q4 全年固定电价链全部在错位时序下求解。
- **修复**：`'0:00+1'` 显式映射到 1440 分钟排到末尾 + **列序指纹断言**（首列 0:10、末列
  24:00，防静默错位复发）。验证：price[0]=0.4248、price[-1]=0.4276 与原始文件逐位一致 ✓。

### 2. 全链重跑后的新数字（全部文件已刷新，以本节为准）

| 项 | 新值 | 备注 |
|---|---|---|
| Q1 购电费 | **35,126.95 元** | 购电量 59,482.70 kWh 不变（日循环唯一解） |
| Q2（λ\*=0.5） | 计划 **15,730,208.52** + 紧急 **478,889.64**（148,700 kWh）= **16,209,098.16** | digest `d4aba1e2…`；λ 网格见 `report/q2_lambda_sweep.csv`（λ=0 合计 16,210,521.20；λ\*=0.5 为全网格最低，紧急费 −55.4%） |
| Q2 完美信息基线 | 12,260,572.90 | `LEGACY_FEE_RECORD` 已更新 |
| Q3 | 电网 **19,244,483.02**（超额惩罚 3,886,041.18 / 退款 65,748.97）+ 紧急 **139,918.92** = **19,384,401.94** | 仅 0:00 基准 16,230,455.83 |
| Q4-2 | **16,375,878.71**（+1.0%） | 计划 15,807,323.14 + 紧急 568,555.57 |
| Q4-3 | **19,479,075.95**（+0.5%） | 电网 19,305,822.47 + 紧急 173,253.48 |
| 季节（334 天口径） | 冬上浮 **+7.0%**、夏 +5.0%、春 −6.9%、秋 −3.0% | 四季样本 92/92/91/59（2.1–12.31） |

### 3. 附带修复（一并完成）
- **季节口径**：`season_split.py` 季节均值改 2.1–12.31（334 天），与全年费用口径闭合（新#3）。
- **A12 四柱 + 表9 加"问题四(二)"列**：`export_plot_data.py` 输出 4 行，R 图与表9 同步（表9/图21 漏 Q4-2 问题）。
- **Q1 数字宏化**：`main.tex` 新增 `\qonekwh`/`\qonecost`；摘要表号"表1、表2"→"表3、表4"（新#4）。
- **"凌晨"→"早晨"**（新#7）：正文 L478 与结果说明。
- **6.5 节"确定性基准=问题二"方向说反**（新#2）：已重写为"完美信息是下界、随机口径高估约 32%"的正确叙事。
- **表9 标题**（新#5）：改为"核心费用指标汇总（问题一为单日基准，问题二/三及问题四重算为全年）"。
- **λ 权衡叙事**：全部宏引用统一（λ\*=0.5、−55.4%、+3.92% 计划费）。
- **图表**：R 5 张（λ 权衡/A9/A10/A11/A12）+ Python（A1–A8、q2_K_sensitivity、q1_plan、q2_cvar_tradeoff、q3_forecast_time、q4_price）全部重绘并同步到 paper/report/根三处。
- **main.pdf**：81 页、0 错误、0 未定义引用。
- **文档同步**：`paper/结果说明.md`、`docs/绘图信息与数据核查.md`（数字表与宏锚点表）、`docs/16.0优化交接说明.md`（头部追加新数字）、`code/main_q2.py` 的 `LAM_OFFICIAL` 注释与 `LEGACY_FEE_RECORD`。

---

## 二、待办（第二批：论文文字/内容类，未修改）

> 按审查报告条目编号，逐项列"改哪里、怎么改"。

| # | 项 | 位置 | 处理要求 |
|---|---|---|---|
| 1 | **B3 表3×2** | `main.tex` 问题二结果分析、问题三结果分析 | 题目硬性要求：补两张"日期 \| 紧急购电时间段 \| 紧急购电量"10 分钟粒度表（只列非零时段；6-21 全零加表注）。数据现成：`report/q2_specdays_Q.csv`（Q2）、`report/result3.xlsx` 紧急购电量 sheet（Q3） |
| 2 | **B2-d / B7.4** | `main.tex` 表4 注 | ①显式交代跨期储能：2.1 起点储电 = 1 月预热后的 1200 kWh（非 6000），日末趋向下界 1200；②"属低谷电价时段释放电能的补电行为"自相矛盾 → 改为"储能在日内高价时段释放、跨日靠低价时段补回的边界运行状态" |
| 3 | **B4** | `main.tex` 表8（tab:season）与数据探索段 | "日电价"列标注来源=**附件4 逐日小时均价**（非附件1）；"峰谷比"标注=**负荷**日峰/谷比（10 分钟时段口径，seasonal_check.py g5）；表注加样本量 92/92/91/90 与单位 |
| 4 | **B5** | `main.tex` 6.3 节 | ANOVA 数据来源"见 result2.xlsx"→"完美信息口径逐日购电费（seasonal_check.py）"，写明样本量/自由度/口径 |
| 5 | **B6** | `main.tex` 6.6 节 + `code/chart_advanced.py` vals | 雷达打分是硬编码。二选一：(a) 补可复算指标表+归一化+权重（含 AHP 判断矩阵 CR<0.1）；(b) 图降级为定性对比，删"锚定明确依据"声称（推荐 b，改动小） |
| 6 | **B7.3** | `main.tex` 表4 注 | 6-21 机理写清：15 天历史均值把工作日负荷带给周六，系统性高估需求 → Q=0 |
| 7 | **B7.5** | `main.tex` 7.3 节（模型不足） | "第 6.3 节的阈值策略即对此的工程回应"→阈值策略在 5.3.2 且未实现。改为"工程上可引入阈值触发策略（见 5.3.2 的建议）"，删除"已回应"暗示 |
| 8 | **B7.6** | `code/lp.py` 第 2 行 docstring | "scipy.integrate"→"scipy.optimize" |
| 9 | **B7.7** | `code/page_check.py` 第 5 行 | 硬编码 E 盘路径 → 改相对路径或命令行参数 |
| 10 | **B7.2 补** | `main.tex` 摘要/5.2.2/6.2 | 计划费上浮 +3.92% 未列入正文，补写三处统一（与 −55.4%/−0.01% 并列） |
| 11 | **新#1** | `main.tex` L300、L903 | "按季节截取历史窗"声称未实现。二选一：删除该句（改"分季解读"），或真实现季节分层情景窗并重跑（工程量大，不推荐） |
| 12 | **新#6** | `main.tex` 附录 A/B | ①results 实为 **5 个**（result1/2/3/4-2/4-3）；②figures 说明补 flow_analysis.png（遗留，未引用）与 q2_cvar_tradeoff.png（存在未引用，建议删除或补引用）；③"共 18 个脚本"→**20 个**（补 q2_K_sensitivity.py、q3_only0_baseline.py 两节） |
| 13 | **B8c（可选）** | `code/main_q3.py` + 论文 5.3.2 | 题目要求"分析是否需要引入其他时刻预报"。若保留阈值策略建议，可真正实现"τ 时点净需求上修幅度超阈值 θ 才启用调整"并给算例对比；否则把 5.3.2 策略段改口为"定性建议" |
| 14 | **B9** | 新增自检脚本 | 输出"论文数字 ↔ 脚本输出 ↔ result xlsx ↔ CSV"对照清单（Q1–Q4 全部关键数），提交前跑一遍全绿 |

---

## 三、验收标准（审查报告原要求，第二批完成后逐条核）

- B3：两张分时段表与结果文件逐位一致；B2：计划购电量列×附件1 电价=计划费，能量守恒（含弃光 R 项）残差<1e-3；B4/B5：表8 每列有来源与计算式、ANOVA 口径可复核；B6：雷达有数值表或已降级；B7：19.2% 已修正为 19.6%（本批已做）、周六/储能注/lp 注释/page_check 路径已改；B9：对照表全绿；main.pdf 编译无报错无未解析引用。
- 注意：第三方审查报告 B2 的"能量不守恒"是**误判**（漏了弃光 R 项，残差=ΣR 恒等式），B8 的"Q3 用 λ*=0.5"也是**误判**（Q3 全程 λ=0），B7.1 的 14.77% 计算基准不明（我方 19.6% 正确）——这三条不要"修复"。

---

## 四、git 状态与复现

- 分支：`17.0优化`（含 `16.0优化` 全部 + 本批修复），未合入 main。
- 复现流水线不变（见 `docs/16.0优化交接说明.md` §五），但**必须先跑 `code/data.py` 的列序断言自检**。
- 提交前记得：报名号占位符、result1 元数据清理（待办清单见 `docs/后续优化待办.md`）。

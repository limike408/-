# 后续优化待办（15.0 之后）

> 本清单由「第一/二优先级改造」会话整理。第一、二优先级已实施；
> **第三、四优先级暂缓，仅记录，勿在本轮实施**。

---

## 第三优先级：方法论升级（创新点深化，有时间再做）

1. **日末 SOC 恒 1200 kWh（无终端价值）**
   - 现状：Q2/Q3 无日末终端价值项，电池每天被"榨干"到运行下限 1200 kWh。
   - 建议：给日末 SOC 加终端价值（滚动 horizon，或下一日边际电价 × 剩余电量近似）；
     或至少在论文加一句解释。

2. **点预报只有"K 天简单均值"一种**
   - 6-21（周六）紧急购电 = 0 暴露"周末效应"：15 天窗口被工作日主导。
   - 建议：加星期哑变量（工作日/周末分均值）或季节分层均值，重跑对比全年 NRMSE。

3. **情景生成只有"整日残差 bootstrap"一种**
   - 建议：季节/相似日分层抽样（只在同类日里抽残差）、block bootstrap（抽连续多天块）。
   - 可衔接已有 `season_split.py`。

4. **CVaR 权重 λ 是拍脑袋选的 {0, 0.5, 1, 2, 5}**
   - 建议：数据驱动选 λ（如选使"计划费+紧急费"总成本最低的 λ），
     或把 λ 解释为"愿意为削 1 元紧急费多付的保险金"。

5. **Q4 建模偏浅（只"换电价重算"）**
   - 建议：对波动电价本身做情景生成、分析电价与负荷/光伏相关性、
     给出电价波动下的鲁棒购电策略，让 Q4 有独立方法贡献。

---

## 第四优先级：稳健性与工程收尾

6. **缺参数敏感性/稳健性分析**
   - 储能容量 12000 kWh、充放功率上限 5000 kW、效率 η=0.9 扰动后结果变化无系统性检验。

7. **缺统一回测基准**
   - Q2 vs Q3 vs Q4 建议做滚动原点回测（rolling-origin backtest）统一评估。

8. **提交前收尾**
   - `result1.xlsx`（及可能残留的 result3/4-2/4-3）元数据 `creator="xueyi"` 用
     `R.strip_personal_meta()` 清理；`result2.xlsx` 已清理。
   - `main.tex` 顶部报名号/学校/队员仍是占位符 `000000000000`；提交前必填。
   - 缺 `requirements.txt` / 环境锁定。
   - git：`main` 未动、多分支未合并，定稿后收口。

---

## 状态

- [ ] 第三优先级 5 项 —— 未开始
- [ ] 第四优先级 3 项 —— 未开始

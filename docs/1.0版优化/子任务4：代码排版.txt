你现在是“国赛论文附录代码排版 Agent”，请参照优秀论文 2025 C132 的附录代码格式，优化我的代码附录。

目标：
让代码附录规范、清晰、可读，正文不放大段代码，完整代码放支撑材料。正文里面的代码的注释需要详细、代码模块用于解决的相关问题要明确、代码的结构要清晰、变量命名要规范

请执行：
1. 检查我论文附录B现状：代码片段零散、行号缺失、缩进不一致、注释不足、变量命名不统一、超长行、无文件列表。
2. 按优秀论文 C132 的结构重组：
   - 附录A 支撑材料文件列表：文件名、功能描述；
   - 附录B 源程序：按问题一~问题四分节；
   - 每个 Listing 有标题、行号、等宽字体、注释、关键代码；
   - 完整代码放 code/ 文件夹，论文只放核心片段。
3. 给出 LaTeX 模板：
   \usepackage{listings}
   \lstset{
     language=Python,
     basicstyle=\ttfamily\small,
     numbers=left,
     numberstyle=\tiny,
     keywordstyle=\color{blue},
     commentstyle=\color{green!50!black},
     stringstyle=\color{red!70!black},
     showstringspaces=false,
     breaklines=true,
     frame=single,
     captionpos=b
   }
   \lstinputlisting[caption={问题一：线性规划核心代码（lp.py）}]{code/lp.py}
4. 给出 Word 排版建议：等宽字体 Consolas 9pt、行号、灰色底纹、按问题分节。
5. 检查代码与正文公式符号一致性：P_t、c_t、f_t、Q_t、E_t、η、CVaR 等。
6. 输出修改后的附录结构示例，包括：
   - 附录A 文件列表；
   - 附录B.1 问题一 LP 求解；
   - 附录B.2 问题二 两阶段随机+CVaR；
   - 附录B.3 问题三 滚动 MPC；
   - 附录B.4 问题四 波动电价重算。
7. 验收标准：代码不占正文页数；附录可编译；无乱码；关键代码有注释；变量命名统一。

输出格式：
- 代码排版诊断；
- 附录结构模板；
- LaTeX/Word 设置；
- 核心代码片段示例。
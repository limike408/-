# 18.0 优化交接说明（给下一位队友）

> 分支：`18.0优化`（基于 `17.0优化`）
> 本文档**整合并取代** `17.0优化交接说明.md`（17.0 的列序修复内容已并入 §一）。
>
> **一句话**：16.0 完成口径改造 → 17.0 修复第三方审查发现的"附件时序环移 10 分钟"根因并全链
> 重跑 → **18.0 复查模型完整度与数据准确性**（修复模型假设旧口径矛盾、补弃光量化、新增三方
> 一致性自检脚本 28/28 全绿）。剩余论文文字/内容类缺陷见 §二。

---

## 一、已完成工作

### A. 16.0（口径改造，详见 `docs/16.0优化交接说明.md`）
- Q2 官方口径升级为**预报式两阶段随机 + CVaR 风险权衡 λ\*=0.5**（数据驱动选值）；
- Q3 重写为**因果滚动随机 MPC**（负荷预报化、情景鲁棒调整 `lp.solve_adjust_scen`、调整时点
  实际储电轨迹、去后见之明），修复旧 `solve_adjust` 的"调整购电免费"退化 bug 与超额重复计价；
- Q4-2/Q4-3 按新口径重算；图表 A9–A12 与 λ 权衡曲线迁移 R(ggplot2)。

### B. 17.0（列序根因修复 + 全链重跑）
- **根因（第三方审查 B1）**：`data.py` 排序函数把 `'0:00+1'`（24:00）标签剥掉 `+1` 后当 0 点
  排序 → **附件1（行）与附件2/4（列）全部环移 10 分钟**（三份附件都错位，不止附件1）。
- **修复**：`'0:00+1'` 显式映射 1440 分钟排到末尾 + **列序指纹断言**（首 0:10、末 24:00，
  防静默错位复发）。验证：`price[0]=0.4248`、`price[-1]=0.4276` 与原始文件逐位一致。
- **附带修复**：季节均值改 2.1–12.31（334 天）口径；A12 补"问题四(二)"四柱、表9 加列；
  Q1 数字宏化 + 摘要表号修正；"凌晨"→"早晨"；6.5 节"确定性基准=问题二"方向纠正；
  全部图表重绘；main.pdf 重编译。

### C. 18.0（本轮：模型完整度与数据准确性复查）
| # | 发现 | 处理 |
|---|---|---|
| 1 | **模型假设仍写旧口径、与正文结果矛盾**：假设 4"问题二完美信息、无需紧急购电"（实际紧急费 478,889.64 元）、假设 5"问题三负荷视为已知"（实际已负荷预报化） | 已改写为现行口径（Q2 预报式 + λ\* 权衡；Q3 只用当时可得信息，负荷前 15 天均值预报） |
| 2 | **"按季节截取历史窗"声称未实现**（2.6 与 6.3 节两处；代码只有"最近 w 天"滑动窗） | 删除声称，改为准确表述 |
| 3 | **弃光量从未量化**：功率平衡反推得 Q2 弃光 **6,255,088 kWh（占光伏 19,068,890 kWh 的 32.8%）**、Q3 **8,596,886 kWh（45.1%）** | 6.5 节补完整量化与机理分析（储能上限使保险性多购无法储存→弃光），模型不足条目补量化，新增宏 `\qtwocurt`/`\qthreecurt`/`\pvtotal` |
| 4 | **λ 敏感性旧数字残留**：6.2 节"42582→44125" | 已更新为 **42505→44124** |
| 5 | 符号表 `E_0=6000` 仅对问题一成立 | 已注明"问题二/三按跨日链滚动" |
| 6 | **缺三方一致性自检**（审查 B9 要求） | 新增 **`code/verify_all.py`**：论文宏 ↔ 脚本复算 ↔ result xlsx ↔ CSV，**28/28 全绿** |
| 7 | B5（ANOVA 来源表述）：6.3 节"数据见 result2.xlsx"口径不符 | 已改为"完美信息口径逐日购电费（`seasonal_check.py`），四季样本 92/92/91/90" |

### D. 最新数字（全部文件已刷新，以此为准）

| 项 | 值 |
|---|---|
| Q1 | 购电量 59,482.70 kWh，购电费 **35,126.95 元** |
| Q2（λ\*=0.5） | 计划 **15,730,208.52** + 紧急 **478,889.64**（148,700 kWh）= **16,209,098.16**；digest `d4aba1e2…` |
| Q2 完美信息基线 | 12,260,572.90 |
| Q3 | 电网 **19,244,483.02**（超额惩罚 3,886,041.18 / 退款 65,748.97）+ 紧急 **139,918.92** = **19,384,401.94**；仅 0:00 基准 16,230,455.83 |
| Q4-2 | **16,375,878.71**（+1.0%） |
| Q4-3 | **19,479,075.95**（+0.5%） |
| 弃光 | Q2 6,255,088 kWh（32.8%）、Q3 8,596,886 kWh（45.1%）、Q4-2 32.8%、Q4-3 44.9% |
| 季节（334 天） | 冬 +7.0%、夏 +5.0%、春 −6.9%、秋 −3.0% |
| 篇幅 | 正文 30 页（p1–p30 含参考文献），附录 p31–p81，共 81 页，0 错误 0 未定义引用 |

### E. 新增/变更文件（本轮相关）
- 新增 `code/verify_all.py`（三方自检，提交前必跑）；
- 修改 `paper/main.tex`（假设节、弃光段、6.2/6.3 节、符号表）、`paper/结果说明.md`（弃光段）；
- `data.py` 列序修复与断言（17.0）。

---

## 二、待办（第二批剩余项，未完成）

> 按"位置 → 处理要求"列出，可直接照做。

| # | 项 | 位置 | 处理要求 |
|---|---|---|---|
| 1 | **B3 表3×2（题目硬要求）** | 问题二、问题三结果分析节 | 补两张"日期 \| 紧急购电时间段 \| 紧急购电量"10 分钟粒度表（只列非零时段；6-21 全零加表注）。数据现成：`report/q2_specdays_Q.csv`（Q2）、`report/result3.xlsx` 紧急购电量 sheet（Q3） |
| 2 | **B2-d / B7.4 表4 注** | `main.tex` L594 附近 | ①显式交代跨期储能：2.1 起点储电 = 1 月预热后 1200 kWh（非 6000），日末趋向下界；②"属低谷电价时段释放电能的**补电**行为"自相矛盾 → 改为"储能在日内高价时段释放、跨日靠低价时段补回的边界运行状态" |
| 3 | **B4 表8 标注** | `main.tex` L284 表头/表注 | "日电价"标注来源 = **附件4 逐日小时均价**；"峰谷比"标注 = **负荷**日峰/谷比（10 分钟口径，`seasonal_check.py` g5）；补样本量与单位 |
| 4 | **B6 雷达图** | `main.tex` 6.6 节 + `chart_advanced.py` 的 `vals` | 打分是硬编码。推荐降级为定性对比并删除"锚定明确依据"的声称；或补可复算指标表 + 归一化 + 权重（含 AHP 判断矩阵 CR<0.1） |
| 5 | **B7.3 6-21 机理** | `main.tex` L592 | "6-21 为周末，负荷低于工作日"→ 写透：15 天历史均值把工作日负荷带给周六，系统性高估需求，故 Q=0 |
| 6 | **B7.5 阈值引用** | `main.tex` L1029 | "第 6.3 节的阈值策略即对此的工程回应"→ 阈值策略在 5.3.2 且**未实现**；改为"工程上可引入阈值触发策略（见 5.3.2 的建议）" |
| 7 | **B7.6 lp.py 注释** | `code/lp.py` 第 2 行 | `scipy.integrate` → `scipy.optimize` |
| 8 | **B7.7 page_check 路径** | `code/page_check.py` 第 5 行 | 硬编码 `E:\desktop\...` → 改相对路径或命令行参数 |
| 9 | **B7.2 计划费上浮** | 摘要 / 5.2 / 5.2.2 / 6.2 | 补写"计划费 +3.92%"（与"紧急费 −55.4%、合计 −0.01%"并列，四处口径统一） |
| 10 | **新#6 附录清单** | `main.tex` 附录 A/B | ①results 实为 **5 个**（result1/2/3/4-2/4-3）；②figures 说明补 `flow_analysis.png`（遗留未引用）与 `q2_cvar_tradeoff.png`（存在未引用）；③脚本数 **18 → 21**（补 `q2_K_sensitivity.py`、`q3_only0_baseline.py`、`verify_all.py` 三节） |
| 11 | **B8c 阈值算例（可选）** | `code/main_q3.py` + 5.3.2 节 | 题目要求"分析是否需要引入其他时刻预报"。若要保留策略建议，可实现"净需求上修幅度超阈值 θ 才启用调整"并给全年费用对比；否则把该段明确为定性建议 |

---

## 三、重要提醒（避免误修）

1. **三条第三方审查项是误判，不要"修复"**：
   - B2"能量不守恒 +734 万 kWh"——那是**弃光量**，恒等式为 ΣP+ΣQ+ηΣc−Σf = Σ(D−G)+**ΣR**；
   - B8"Q3 的 P 用 λ\*=0.5、调整用 λ=0 导致口径污染"——Q3 **全程 λ=0**（`main_q3.py` 明确
     `lam=0.0`），不存在该问题；
   - B7.1"19.2% 应为 14.77%"——按论文自身数字为 **19.6%**（317.5 万 / 1620.9 万），审查者基准不明。
2. **提交前必跑** `cd code && python -X utf8 verify_all.py`（应输出 28/28 通过）。
3. 改数字只需改 `paper/main.tex` 顶部第 13–35 行的 `\newcommand` 宏块（全文唯一来源），
   改完同步 `paper/结果说明.md` 与 `docs/绘图信息与数据核查.md`。
4. 每次覆盖结果附件前先备份（审查要求保留可回溯证据）。
5. 提交前检查：报名号/学校/队员占位符、电子版 `[withoutpreface,bwprint]`、`result1.xlsx`
   元数据清理（见 `docs/后续优化待办.md`）。

---

## 四、复现流水线（约 100 分钟）

```powershell
cd code
python -X utf8 q2_lambda_sweep.py     # ① λ 网格 → q2_lambda_sweep.csv（确认 λ*=0.5）
python -X utf8 main_q2.py             # ② Q2 官方 → result2.xlsx（digest 校验）
python -X utf8 main_q3.py             # ③ Q3 → result3.xlsx + 对比 CSV/图
python -X utf8 main_q4.py             # ④ Q4-2/Q4-3 → result4-*.xlsx
python -X utf8 season_split.py        # ⑤ 季节（334 天口径）
python -X utf8 chart_advanced.py      # ⑥ A1–A8
python -X utf8 q2_K_sensitivity.py    # ⑦ K 敏感性
python -X utf8 export_plot_data.py    # ⑧ plot_annual.csv（4 行）
python -X utf8 verify_all.py          # ⑨ 三方自检（必须 28/28）
cd ..   # ⑩ R 绘图（工作目录=仓库根）
& 'C:\Program Files\R\R-4.6.1\bin\Rscript.exe' scripts_r/plot_lambda_tradeoff.R 0.5
& 'C:\Program Files\R\R-4.6.1\bin\Rscript.exe' scripts_r/plot_A9_cvar.R 0.5
& 'C:\Program Files\R\R-4.6.1\bin\Rscript.exe' scripts_r/plot_A10_season.R
& 'C:\Program Files\R\R-4.6.1\bin\Rscript.exe' scripts_r/plot_A11_roll.R
& 'C:\Program Files\R\R-4.6.1\bin\Rscript.exe' scripts_r/plot_A12_annual.R
cd paper && xelatex -interaction=nonstopmode main.tex && xelatex -interaction=nonstopmode main.tex
```

> 图表同步规则：R 产物（λ 权衡、A9–A12）为**权威版本**，需从 `paper/figures_adv/` 回写
> `figures_adv/` 与 `report/figures_adv/`；Python 产物（A1–A8、基础图）从 `figures_adv/`、
> `report/figures/` 同步到 `paper/`。顺序颠倒会导致 R 图被 Python 旧版覆盖。

---

## 五、相关文档索引

| 文档 | 内容 |
|---|---|
| `docs/18.0优化交接说明.md` | 本文档（最新总交接） |
| `docs/17.0优化交接说明.md` | 17.0 列序修复批次（内容已并入本文档 §一B） |
| `docs/16.0优化交接说明.md` | 16.0 口径改造详情 + 完整复现流水线 |
| `docs/绘图信息与数据核查.md` | 插图-数据对照手册（供多模态模型逐图核查，含各图应有数字） |
| `docs/后续优化待办.md` | 第三/四优先级（方法论升级与工程收尾） |
| `paper/结果说明.md` | 每题结果口径、数值来源与全部结果表 |
| `Q2口径切换交接说明.md` | 历史（Q2 首次口径切换，已被取代） |

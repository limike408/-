# 交接提示词：C 题 Q2 口径切换与情景生成升级（执行 Agent 用）

【任务类型】编写代码（Python）——重写 Q2 官方求解流程，不涉及论文修改。

【赛题背景】2026 全国大学生数学建模竞赛 C 题"微网与外部电网电力调控策略"。微网含光伏 + 储能 + 外网购电，以 10 分钟为时段（每天 144 时段），目标最小化购电费。储能参数（附录1）：电量须保持 1200~10800 kWh（最大容量 12000，但运行界 10800）；最大充放电功率 5000 kW（每时段能量上限 = 5000×1/6h = 833.33 kWh）；充放效率各 90%（η=0.9，双向，往返 81%）；2025-01-01 0:00 储电量 6000 kWh；储电量跨日连续。Q2 规则：每天 0:00 制定计划购电 P（用附件1 固定电价 + 附件2 数据），当天供电不足（微网供电低于负荷）的部分按交易时刻电价 5 倍紧急购电 Q。结果文件 result2.xlsx 输出 2025-02-01 至 12-31（2025 年 1 月仅作储能状态预热，不输出）。

【当前状态】队友已完成全套代码（仓库 code/：config.py / data.py / lp.py / scenarios.py / results.py / main_q1~q4.py），Q2 现有官方结果用的是"完美信息"口径（把附件2 实际负荷/光伏当作 0:00 计划依据 → 全年 Q≡0 → result2.xlsx 紧急购电量表全空、论文表3 全零，有评审风险）。已讨论决定：**Q2 官方口径切换为"预报式两阶段随机规划"，表3 填真数**。

【已确定的模型】（全部参数已拍板，请严格按此实现）
1. 点预报：负荷、光伏分别取"过去 K 天逐槽（144 时段）均值"。**冷启动**：2025-01-01 无历史，用附件1 的负荷/光伏剖面（data.load_attach1() 的 load/pv）作为该日点预报与情景基准；此后第 d 天历史不足 K 天时，取 min(K, 可用天数) 天均值。
2. 情景生成（升级版，替代原逐槽独立 bootstrap）：残差池 = 过去 K 天每一天的（实际 − 点预报）整条曲线；有放回抽 S=30 个整天（负荷与光伏必须同一天成对抽）；情景 = 点预报 + 整日残差。注意：造第 d 天情景只能用 d 之前的数据（信息因果，禁止用到未来）。
3. K 选择（数据驱动，两层）：候选 K∈{7,15,30}。交叉验证：对 2025-02-01~12-31 每一天 d，用其前 K 天逐槽均值预报 d，计算 144 时段 RMSE 并除以该日实际均值做归一化。**①全局层**：全年平均，负荷、光伏各自选最优 K（K_L_global、K_P_global）。**②分季层**：按月份分季（冬=12/1/2月、春=3/4/5月、夏=6/7/8月、秋=9/10/11月；评估仅含 2.1~12.31 天数，冬季=2月+12月），每季各自选 K。输出"全局 vs 分季"全年平均 NRMSE 对照表；若分季 NRMSE 更低，官方运行采用分季 K（每日按所在季节取 K），否则用全局 K，并在控制台打印最终采用的方案与理由。
4. 计划 P：复用 lp.solve_two_stage(price, scenD, scenG, E_start, lam=0.0, beta=0.90)（两阶段随机 LP，已验证实现正确，勿改 lp.py）：第一阶段 P 全情景共享、成本 Σp·P；第二阶段每情景独立 c/f/Q/R/E，期望紧急费 (1/S)ΣΣ5p·Q。官方结果 lam=0（纯期望最小化）。
5. 结算：固定 P，用当天实际负荷/光伏调用 lp.solve_day_fixedP（紧急价 5p）→ 得当日实际 Q、c、f、E_end；E_end 传给次日（跨日连续）。
6. 全年链：2025-01-01 起 E=6000 逐日滚动；只输出 2025-02-01 起的结果。
7. 随机性：抽样用 np.random.default_rng(2026 + 日期序号)，与日期绑定、确定性、重跑逐位一致。
8. λ 敏感性分析（method_analysis）保留，但改用新情景生成器：指定日期 2025-03-20 / 06-21 / 09-23 / 12-21，λ∈{0,0.5,1,2,5}，输出 plan_fee / emerg_fee / total / emerg_kwh 表（CSV）。

【环境与路径】仓库根目录 = C:\Users\NAE\Desktop\数学建模\2026。运行前必须修改 code/config.py 的路径（原 C_BASE 指向不存在的 E 盘）：
  C_BASE  = r'C:\Users\NAE\Desktop\数学建模\2026'
  DATA_DIR = os.path.join(C_BASE, 'data')
  TPL_DIR  = os.path.join(DATA_DIR, '附件5模板')
  RES_DIR  = os.path.join(C_BASE, 'report')
  FIG_DIR  = os.path.join(C_BASE, 'report', 'figures')
依赖已装：numpy / pandas / scipy / openpyxl / matplotlib。

【要改的文件】
1. code/scenarios.py：新增 residual_scenarios(load_hist, pv_hist, K, S=30, seed)（旧函数保留不删）。
2. code/main_q2.py：新增交叉验证选 K 函数；重写官方循环 run()；method_analysis() 改用新生成器；旧完美信息口径函数保留但改名（如 perfect_info_legacy）并注释说明"旧口径，仅作对比"。
3. 禁止改动：lp.py、data.py、results.py 核心逻辑（只能新增函数）；禁止运行/改动 main_q1/q3/q4 与 chart 脚本；禁止 git commit；禁止改 paper/。

【输出与验收】
1. 先备份旧结果：report/result2.xlsx 复制为 report/result2_完美信息备份.xlsx，再用 R.write_result2 写新结果（计划购电量/充放电量/紧急购电量三张表填全，紧急购电量表应有真实非零行）。
2. report/q2_new_summary.csv：全年计划购电费、全年紧急购电费、合计、K 方案（全局/分季）与所选 K 值（K_L、K_P）、四指定日期各自的紧急电量(kWh)与紧急费(元)。
3. report/q2_specdays_Q.csv：四指定日期 × 144 时段完整 Q 序列（kWh/时段），供论文表3 使用。
4. 控制台打印：上述全部数字 + "全局 vs 分季"NRMSE 对照表 + 旧完美信息计划费 12,259,844.62 元与新计划费的差异及一句解释。
5. 程序内断言：每日 E 全程 ∈ [1200,10800]；seed 固定重跑逐位一致。
6. 运行：cd code && python main_q2.py。全年 365 天 × 30 情景 LP（单次约 2 万变量），预计数分钟至半小时，加进度打印（每 30 天打一行），如超时可用后台运行。

【验收标准（回传后教练将逐条核对）】
- 全年紧急购电费 > 0 且量级合理（预期几十万元级）；四日表3 Q 全非零；
- E 不越界；重跑逐位一致；K_L、K_P 明确打印；
- 新旧计划费差异有合理解释（预报式计划应略保守，因保险性多购）。

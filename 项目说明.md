# C 题（2026 数学建模）项目全文件介绍与快速上手指南

本文件面向**任何需要接手本项目的人**：介绍仓库里每一个文件是做什么的、目录怎么组织、
以及如何一步一步把代码跑通、复现论文中的所有结果。

---

## 一、项目是解决什么问题的

**题目背景**：一个含 **光伏 + 储能 + 电网购电** 的微网，需要以 **10 分钟为时段**（每天
`1 44` 个时段）制定购电与充放电计划，目标是在满足负荷供电、储能运行约束的前提下**最小化购电费用**。

**逐问递进**（难度由低到高）：
| 问题 | 不确定性程度 | 目标 |
|---|---|---|
| 一问 | 完美信息（电价/光伏/负荷全已知） | 确定性单日线性规划，最小当日购电费 |
| 二问 | 光伏/负荷逐时波动 | 两阶段随机规划 + CVaR 风险刻画 |
| 三问 | 多时刻光伏预报不断更新 | 滚动随机模型预测控制（MPC）+ 调整违约/超额惩罚 |
| 四问 | 电价改为全年波动（附件4） | 用波动电价重算问题二、三 |

**核心建模路线**：① "计划购电—滚动调整—紧急购电"三层滚动随机模型统一框架；
② CVaR 嵌入两阶段随机规划，量化"以少量计划费换取紧急购电削减"的风险—经济权衡；
③ 季节分型分析（冬季购电费约为春季 1.56 倍，ANOVA F=28.7）。
论文以"供电可靠性为主、经济性为辅"为统一优化目标（19.0 优化起）。

---

## 二、仓库目录总览

```
C题/
├── README.md                  # 协作规范 + 本项目摘要（GitHub 首页显示）
├── 项目说明.md                 # 本文档：全文件介绍 + 快速上手
│
├── code/                       # ★ 全部 Python 求解与分析脚本
│   ├── config.py               #   统一配置（路径/储能参数/电价机制/随机参数）
│   ├── data.py                 #   附件1-4 数据加载 → 统一 numpy
│   ├── lp.py                   #   核心 LP 求解器（能量平衡 + 储能递推）
│   ├── scenarios.py            #   情景生成（bootstrap 历史残差）
│   ├── results.py              #   结果写入（按附件5模板填 Excel）
│   ├── main_q1.py              #   第1问入口
│   ├── main_q2.py              #   第2问入口（官方结果 + CVaR 方法层）
│   ├── main_q3.py              #   第3问入口（滚动 MPC）
│   ├── main_q4.py              #   第4问入口（波动电价重算）
│   ├── emergency_analysis.py   #   紧急波动定义与全年统计（19.0 新增）
│   ├── chart_4_0.py            #   4张补充图（A9-A12）
│   ├── chart_advanced.py       #   8张高级图（A1-A8，季节/帕累托/雷达/桑基等）
│   ├── seasonal_check.py       #   季节差异实证
│   ├── season_split.py         #   问题四季节费用拆分
│   ├── extract_paper_day.py    #   抓取指定日详细结果（供论文表格）
│   ├── extract_paper_nums.py   #   抓取论文全部数值
│   └── page_check.py           #   统计 main.pdf 页码分布
│
├── data/                       # 原始数据 + 结果模板
│   ├── 附件1.xlsx              #   单日 144 时段电价/光伏/负荷预报
│   ├── 附件2.xlsx              #   全年 365×144 负荷/光伏 实际值
│   ├── 附件3.xlsx              #   全年光伏整点预报
│   ├── 附件4.xlsx              #   全年 366×144 波动电价
│   └── 附件5模板/              #   官方结果回填模板（result1/2/3/4-2/4-3.xlsx）
│
├── paper/                      # ★ 论文与配套
│   ├── main.tex                #   论文源文件（XeLaTeX，cumcmthesis 模板）
│   ├── main.pdf                #   已编译论文（77 页）
│   ├── cumcmthesis.cls         #   数学建模官方模板类
│   ├── simkai.ttf / simsun.ttc #   中文字体
│   ├── C题_题目.pdf            #   赛题原文
│   ├── 结果说明.md              #   ★ 每题结果口径与数值来源
│   ├── AI工具使用详情.tex/.pdf  #  AI 工具使用详情支撑材料
│   ├── figures/                #   4张主图（q1_plan 等）
│   └── figures_adv/            #   12张进阶图（A1-A12）
│
├── report/                     # 结果与图表（复盘用）
│   ├── result1.xlsx            #   第1问结果
│   ├── result2.xlsx            #   第2问结果
│   ├── result3.xlsx            #   第3问结果
│   ├── result4-2.xlsx          #   第4问·重算问题2
│   ├── result4-3.xlsx          #   第4问·重算问题3
│   ├── *.csv                  #   对比数据（两阶段/滚动/季节拆分）
│   ├── figures/                #   基础图
│   └── figures_adv/            #   进阶图
│
├── docs/                       # 各版优化建议与流程图（1.0~10.0 版）
│   ├── 1.0版优化/ ... 6.0优化/  #   早期优化建议与流程图
│   ├── 7.0优化/                #   正文+附录重构
│   ├── 8.0优化/                #   AI 使用说明
│   ├── 9.0优化/                #   去承诺书、建模小节层级
│   └── 10.0优化/               #   ★ 流程图重绘脚本 + 备份
│
└── 全国大学生数学建模竞赛*.pdf   # 赛会官方规定（AI 使用 / 系统手册）
```

---

## 三、文件逐个介绍

### 3.1 `code/` —— 建模代码（核心）

**模块分层**（由底到顶）：

| 文件 | 作用 | 关键函数/常量 |
|---|---|---|
| `config.py` | **全局配置**，所有路径、储能参数、电价机制、随机规划参数 | `T_IN_DAY=144`、`SOC_MIN/MAX`、`ETA=0.9`、`EMERG_MULT=5`、`setup_plot_style()` |
| `data.py` | **附件数据加载**，把 附件1-4 读成 `(日期×144)` 的 numpy 统一格式 | `load_attach1/2/3/4()` |
| `lp.py` | **核心求解器**（scipy `linprog`/HiGHS），一句话描述能量平衡约束 | `solve_day(...)`，内部变量 `[P,c,f,Q,R,E]` |
| `scenarios.py` | **情景生成**，非参数 bootstrap 历史残差 | `forecast_and_scenarios(load_hist,pv_hist,S=30,seed=1)` |
| `results.py` | **结果写入**，按 附件5 模板用 openpyxl 填值 | `_fill_plan_sheet` 等 |

**四个问题入口**（自上而下调用模块层）：

| 文件 | 对应问题 | 求解内容 | 输出 |
|---|---|---|---|
| `main_q1.py` | 第1问 | 完美信息确定性 LP，储能日循环（24:00=0:00=6000kWh） | `result1.xlsx`（计划购电 + 充放电两张表）+ `q1_plan.png` |
| `main_q2.py` | 第2问 | A.官方结果（当日实际后验完美信息）；B.两阶段随机+CVaR 方法层 | `result2.xlsx` + `q2_cvar_tradeoff.png` |
| `main_q3.py` | 第3问 | 0/6/12/18 滚动 MPC + 违约50%/超额150% 惩罚 | `result3.xlsx`（4张表）+ `q3_forecast_time.png` |
| `main_q4.py` | 第4问 | 波动电价（附件4）重算问题2/3 | `result4-2.xlsx`、`result4-3.xlsx` + `q4_price.png` |

**图表与分析脚本**（独立低开销，不重新求解）：

| 文件 | 用途 | 输出 |
|---|---|---|
| `chart_advanced.py` | 8张季节/风险进阶图（A1-A8） | `figures_adv/A1~A8.png` |
| `chart_4_0.py` | 4.0 版补充图 A9-A12 | `figures_adv/A9~A12.png` |
| `seasonal_check.py` | 季节差异 ANOVA 实证 | 控制台/表格 |
| `season_split.py` | 问题四 固定/波动电价 × 季节 日费用拆分 | `report/season_cost_split.csv` |
| `extract_paper_nums.py` | 抓取论文全部数值 | 供 tex 引用 |
| `extract_paper_day.py` | 抓取指定4天详细结果 | 供论文表格 |
| `page_check.py` | 统计 main.pdf 页码分布 | 控制台 |

> **运行注意**：`config.py` 中 `C_BASE = r'E:\desktop\2026建模\C题'` 是**硬编码绝对路径**。
> 换电脑必须全局改这一行把路径指到你的实际目录，否则会因找不到附件而报错。

### 3.2 `data/` —— 数据

| 文件 | 说明 |
|---|---|
| 附件1.xlsx | 单日 144 时段：电价（元/kWh）、光伏预报、负荷预报 |
| 附件2.xlsx | 全年 2.1~12.31，负荷/光伏 **实际**值（365×144） |
| 附件3.xlsx | 全年光伏**整点预报** |
| 附件4.xlsx | 全年**波动电价**（366×144 含1月1日） |
| 附件5模板/ | 官方规定的结果回填格式 |

### 3.3 `paper/` —— 论文

| 文件 | 说明 |
|---|---|
| `main.tex` | **论文 LaTeX 源**。用 XeLaTeX 编译（见下文）。引用了大量 `figures/`、`figures_adv/` 图 |
| `main.pdf` | 已编译论文（77 页，正文≤30 页 + 附录源码 34 页） |
| `cumcmthesis.cls` | 数学建模官方 LaTeX 模板类（v2.7） |
| `simkai.ttf`/`simsun.ttc` | 中文字体（编译必需） |
| `结果说明.md` | **每题结果口径、数值来源、详细结果表格**——想快速了解结果看这份 |
| `AI工具使用详情.tex/.pdf` | AI 使用声明支撑材料 |

> 论文征文后**提交前需填写** `main.tex` 顶部 L31-L41 的占位符：
> `\baominghao`、`\schoolname`、`\membera/b/c`、`\supervisor`、日期等（当前是 `000000000000`）。
> 电子版提交时把第1行改为 `\documentclass[withoutpreface,bwprint]{cumcmthesis}`。

### 3.4 `report/` —— 结果与图表（复盘）

与 `data/附件5模板/` 里的结果一一对应（`result1/2/3/4-2/4-3.xlsx`），另含 `figures/`（基础）、
`figures_adv/`（进阶）与对比 CSV。**注意**：`report/` 和 `paper/figures*`、`code` 生成的
`figures/`、`figures_adv/` 内容一致——`code` 负责生成，`paper`/`report` 负责存放。

### 3.5 `docs/` —— 版本优化记录

| 版本 | 主题 |
|---|---|
| 1.0~6.0 优化 | 创新点提升、篇幅结构、图表质量、代码排版、后续多轮优化 |
| 7.0 优化 | 附录展开全部 16 个源码（B.1~B.16） |
| 8.0 优化 | 加入 AI 工具使用说明 + 支撑材料 |
| 9.0 优化 | 删除承诺书（`withoutpreface`）、建模小节层级细化 |
| 10.0 优化 | ★ 流程图重绘（每块≤6字、字大）：`10.0优化/make_flow.py` |
| 各版 `建议.txt` | 每轮优化的**具体需求清单**（看这个能快速了解每轮改了什么） |

---

## 四、快速上手（快速复现全部结果）

### 步骤 0：准备环境

```bash
# 安装依赖（Python 3.9+）
pip install numpy pandas scipy openpyxl matplotlib pdfplumber PyPDF2

# 改路径（唯一必须改的地方）
#   打开 code/config.py，把第 6 行
#   C_BASE = r'E:\desktop\2026建模\C题'
#   改成你的实际目录。
```

> 数据文件（附件1-4.xlsx、附件5模板/）必须在 `config.py` 指向的目录下，
> 目录组织见第二节。

### 步骤 1：按顺序跑四个主入口，复现每题结果

```bash
cd code
python main_q1.py     # → 生成 result1.xlsx、figures/q1_plan.png
python main_q2.py     # → 生成 result2.xlsx、figures/q2_cvar_tradeoff.png
python main_q3.py     # → 生成 result3.xlsx、figures/q3_forecast_time.png
python main_q4.py     # → 生成 result4-2.xlsx、result4-3.xlsx、figures/q4_price.png
```

跑完后，`report/result*.xlsx` 与论文正文数值**逐位一致**。

### 步骤 2：生成进阶图与季节分析（可选）

```bash
python chart_advanced.py   # → figures_adv/A1~A8.png
python chart_4_0.py        # → figures_adv/A9~A12.png
python emergency_analysis.py  # → 紧急波动全年统计 + emergency_series/season.png
python seasonal_check.py   # → 季节 ANOVA 实证
python season_split.py     # → report/season_cost_split.csv
python extract_paper_nums.py   # → 抓取论文数值
python extract_paper_day.py    # → 抓取指定日结果
```

### 步骤 3：编译论文 PDF

```bash
# 进入 paper 目录，用 XeLaTeX 编译两遍（二遍收敛交叉引用）
cd paper
xelatex -interaction=nonstopmode main.tex
xelatex -interaction=nonstopmode main.tex   # 第二遍消除 ?? 未定义引用
```

> 依赖：XeLaTeX（MiKTeX/TeX Live）、`cumcmthesis.cls`、中文字体 `simkai.ttf`、`simsun.ttc`。

---

## 五、关键数值速查（论文摘要口径）

| 指标 | 数值 |
|---|---|
| Q1 全天计划购电量 | 59 482.7 kWh |
| Q1 购电费 | 35 126.85 元 |
| Q2 全年计划购电费 | 15 726 290.43 元（预报式两阶段随机，λ\*=0.5） |
| Q2 全年紧急购电费 | 478 538.57 元（148 179.5 kWh） |
| Q2 全年合计 | 16 204 829.00 元 |
| Q3 电网费 + 紧急购电费 | 22 460 575.77 + 75 922.06 元，合计 22 536 497.83 元（18.0 因果口径） |
| Q4-2（波动电价重算Q2） | 15 802 243.35 元（较固定电价 +0.5%，合计 +1.0%） |
| Q4-3（波动电价重算Q3） | 22 883 518.51 元（+1.5%） |
| 季节差异 | 波动电价风险夏季最大（+9.3%） |

> 完整过程与口径细节见 [paper/结果说明.md](paper/结果说明.md)。

---

## 六、常见问题 FAQ

**Q：为什么 `code/` 生成的图与 `paper/`、`report/` 里的一样？**
A：同一批图，`code` 负责生成、`paper`/`report` 负责存放（论文引用 `paper/figures`）。

**Q：跑脚本报 `FileNotFoundError` 找不到附件？**
A：改动 `config.py` 的 `C_BASE` 指向你的实际数据目录，并确认附件在 `data/`。

**Q：哪些文件是提交前必须改的？**
A：论文 `main.tex` 的报名号/队号/成员/学校占位符，以及确认电子版用 `[withoutpreface,bwprint]`。

**Q：main.pdf 编译出来有 `??`？**
A：需要**跑两遍** xelatex 收敛交叉引用；还可能是 `.aux` 缓存没清，删掉 `.aux` 再编译。

---

## 七、目录里刻意不跟踪了什么

见 [.gitignore](.gitignore)：LaTeX 编译垃圾（*.aux/*.log）、Python 缓存（`__pycache__`）、
Office 临时锁文件（`~$*`）、本地配置（`.vscode/`）等一律不入库，仓库只保留正式成果。